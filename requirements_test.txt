# Brings in Home Assistant itself plus pytest, pytest-asyncio and pytest-socket
# at versions matching the Home Assistant release it targets.
pytest-homeassistant-custom-component

# Linting and formatting; pinned loosely so CI matches local runs.
ruff>=0.16.5

pytest-cov
